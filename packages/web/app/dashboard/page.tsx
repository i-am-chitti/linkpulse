/**
 * Placeholder for now: the link list, create form, and per-link analytics
 * are the next chunk. This proves the auth shell end to end - login, a
 * protected route, the session surviving a reload, and logout.
 */
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-gray-900">Your links</h1>
      <p className="mt-2 text-gray-600">Link management is coming in the next chunk.</p>
    </div>
  );
}
